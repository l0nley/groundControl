<UserControl x:Class="GroundControl.Station.EditableLabel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GroundControl.Station"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="100" 
             DataContext="{Binding RelativeSource={RelativeSource Self}}" >
  <UserControl.Resources>
    <local:ExtendedBoolToVisibilityConverter x:Key="boolToVisibility" />
  </UserControl.Resources>
  <Grid>
    <TextBox Text="{Binding Text, Mode=TwoWay}" Visibility="{Binding EditMode, Converter={StaticResource boolToVisibility}}"  
             KeyDown="TextBox_KeyDown"
             local:InputBindingsManager.UpdatePropertySourceWhenEnterPressed="TextBox.Text"/>
    <Label Content="{Binding Text}" FontWeight="DemiBold"
           Visibility="{Binding EditMode, Converter={StaticResource boolToVisibility}, ConverterParameter=1}" 
           MouseDoubleClick="Label_MouseDoubleClick"/>
  </Grid>
</UserControl>
