<UserControl x:Class="GroundControl.Station.Components.Connection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GroundControl.Station.Components"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <Grid>
    <Grid.Resources>
      <BooleanToVisibilityConverter x:Key="cnv" />
    </Grid.Resources>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="Auto" />
    </Grid.ColumnDefinitions>
    <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding Name}" />
    <Button Grid.Row="0" Grid.Column="1" Style="{StaticResource BtnCollapse}" Click="Button_Click" />
    <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Text="{Binding TypeName}" Visibility="{Binding NotCollapsed, Converter={StaticResource cnv}}" />
    <TextBlock Grid.Row="2" Grid.ColumnSpan="2" Text="{Binding Uri}" Visibility="{Binding NotCollapsed, Converter={StaticResource cnv}}" />
  </Grid>
</UserControl>
